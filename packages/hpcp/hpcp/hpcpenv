#!/bin/bash

host=${NERSC_HOST}

if [ "x$1" = "xset" ]; then
    set | awk -F '=' '! /^[0-9A-Z_a-z]+=/ {exit} {print "export", $0}' > ${HOME}/.hpcpenv_${host}
elif [ "x$1" = "xclear" ]; then
    rm -f ${HOME}/.hpcpenv_${host}
else
    echo "Usage:  $0 [ set | clear ]"
fi

